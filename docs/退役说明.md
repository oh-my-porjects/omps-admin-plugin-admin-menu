# admin-menu 退役说明

`admin-menu` 在 V1-V3 中承担菜单和 Spec 存储。该职责已迁移到 V4 完整发布集：admin-server 生成并发布完整 Spec，runtime 原子替换保存并按照权限裁剪，project-admin-web 使用同一份产物渲染。

因此本模块不再提供构建、部署、接口或页面配置。任何新后台功能都必须进入 V4 正式链路，不能在本仓库补回旧协议。

历史 `/api/admin-menu/*` 与 `/api/admin-meta/*` 路由及其接口契约已经移除。它们不属于当前运行协议，本仓库也不提供替代接口或兼容接口。
